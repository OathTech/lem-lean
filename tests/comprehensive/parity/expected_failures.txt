# Parity probes whose divergence is KNOWN (format: <probe>,<reason>).
# run.sh requires each listed probe to FAIL; a listed probe that passes
# fails the suite (stale entry). Two entry classes, and no third: (1) a
# registered fix in flight — the entry names the fix that will delete it;
# (2) a RULED OCaml-target deviation — the OCaml reference deviates from
# lem's own (prover-side) semantics and the Lean target follows lem, so
# parity is not required in the OCaml→Lean direction; the entry cites the
# ruling record. Never a home for undocumented divergences.
p_str_bytes,F2 strings-are-bytes — design note doc/lean-backend/2026-09-03_string-representation-design.md; implementation is the last slice of the arc (L)
p_str_escapes,F2 strings-are-bytes (unicode literal row) — same fix as p_str_bytes
f_int_of_big_num,RULED OCaml-target deviation ([USER 2026-09-03] "ocaml limits that are hardcoded thanks to ocaml-level execution issues are also forbidden, the real thing is the logical semantics"): Nat_big_num.to_int raises Failure "int_of_big_int" at 2^62 on the OCaml target; lem's nat/int are unbounded and the Lean conversions are the identity — doc/lean-backend/2026-09-03_exception-case-rulings.md X3 addendum (2026-09-04)
