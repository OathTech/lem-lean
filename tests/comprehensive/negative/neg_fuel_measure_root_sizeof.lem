(* EXPECT: FM-root *)
(* pre-merge audit M1: `_root_.sizeOf l` evaded the head-only FM-sizeOf
   check (caught only by Lean: Unknown identifier). *)
open import Pervasives
val len : list nat -> nat
let rec len l = match l with | [] -> 0 | _ :: xs -> 1 + len xs end
declare {lean} fuel val len = `999`
declare {lean} fuel_measure val len = `_root_.sizeOf l`
