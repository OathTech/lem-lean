(* EXPECT: Lean backend: cannot derive BEq/Ord instances for type 't1': heterogeneous type-parameter counts put its mutual block in the Type 1 universe (the historical sorry-bodied residual instances are deleted, arc-10 audit fix); escape hatches: 'declare {lean} skip_instances type t1' plus hand-written Lean instances where demanded, or 'declare lean target_rep type t1' mapping it to a hand-written Lean type *)
open import Pervasives
(* NEGATIVE probe (arc-10 audit fix, auditor A F1): heterogeneous
   type-parameter counts put this mutual block in Lean's Type 1
   universe, where the historical residual was a pair of sorry-bodied
   BEq/Ord instances — an opaque-inhabitant emission path (sorryAx),
   banned since arc-8. The fixed backend must FAIL CLOSED at
   generation time with an error naming the type and both escape
   hatches (asserted verbatim by the EXPECT header above), exactly
   like the Inhabited fail-closed path. The comparison demand below
   (t1 equality) documents the use that would have silently completed
   through the sorried instance. *)

type t1 = K1 | K2 of t2 nat
and t2 'a = L1 of 'a | L2 of t1

let probe : bool = (K1 = K1)
