open import Pervasives_extra
val cap2 : maybe (maybe nat) -> maybe nat
let cap2 x = match x with
  | Just none -> none
  | Nothing   -> Nothing
end
let cap2_result = cap2 (Just (Just 5))
val cap3 : bool -> nat
let cap3 x = match x with
  | true -> 1
  | false -> 0
end
